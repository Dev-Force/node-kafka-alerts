{
    "apps": [
        {
            "name": "notifications",
            "script": "./dist/index.js",
            "merge_logs": true,
            "max_restarts": 20,
            "instances": 1,
            "max_memory_restart": "200M",
            "exec_mode": "cluster",
            "env": {
                "PORT": 3000,
                "NODE_ENV": "production"
            }
        }
    ]
}
